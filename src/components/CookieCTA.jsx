const CookieCTA = ({ title }) => {
  return <button className="bg-white rounded-lg p-3 hover:-translate-y-1 hover:duration-200 hover:transition-all dark:text-white dark:bg-black">{title}</button>;
};

export default CookieCTA;
